[{{ project.name|slice:":20" }}] {{ instance.author }}{% if is_comment %}{{ _(' posted a new comment at ') }}{{ instance.page.title }}{% else %}{{ _(' created a new task: ') }}{{ instance.title }}{% endif %}{{ _('.') }}
